from django.apps import AppConfig  # Importar la clase AppConfig de Django

class InventarioAppConfig(AppConfig):
    default_auto_field = 'django.db.models.BigAutoField'
    name = 'inventario_app'

